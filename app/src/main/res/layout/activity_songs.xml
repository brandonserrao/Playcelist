<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:theme="@style/AppTheme">

    <androidx.appcompat.widget.SearchView
        android:id="@+id/sv_songs"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_toStartOf="@+id/clear_search_songs"
        android:background="@color/Surface" />

    <ImageView
        android:id="@+id/clear_search_songs"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_alignBottom="@+id/sv_songs"
        android:layout_alignParentRight="true"
        android:background="@color/Surface"
        android:onClick="onClickClearSearch"
        android:src="@drawable/refresh" />

    <!-- <EditText
        android:id="@+id/edittext_searchbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/sv_songs"
        android:layout_toStartOf="@+id/search_button"
        android:hint="@string/s_name" />

    <Button
        android:id="@+id/search_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/sv_songs"
        android:layout_alignParentEnd="true"
        android:onClick="searchSongsByName"
        android:text="Search" /> -->

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_songs"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/bottom_navigation"
        android:layout_below="@+id/sv_songs" />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        app:menu="@menu/bottom_nav_menu" />
</RelativeLayout>